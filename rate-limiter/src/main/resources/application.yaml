spring:
  application:
    name: rate-limiter
  data:
    redis:
      host: localhost
      port: 6379

rate-limiter:
  limit: 5
  window-ms: 10000

management:
  endpoints:
    web:
      exposure:
        include: prometheus,health,info